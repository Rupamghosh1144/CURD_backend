<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title>Document</title>
</head>
<body class="bg-zinc-900 text-white">
  
  <!-- Top Navigation -->
  <div class="w-full p-4 border-b border-zinc-800 flex justify-end">
    <a class="text-blue-400 hover:text-white transition-colors" href="/read">Read Users</a>
  </div>

  <!-- Centered Form -->
  <div class="w-full min-h-[calc(100vh-64px)] flex justify-center items-center">
    <form action="/update/<%= user._id %>" method="post" class="flex flex-col gap-4 bg-zinc-800 p-6 rounded-lg shadow-lg w-80">
      <h1 class="text-2xl font-bold text-center mb-2">Edit User</h1>
      <input class="px-5 py-2 bg-transparent border-2 border-zinc-700 rounded-lg outline-none" type="text" placeholder="Name" name="name" value="<%= user.name %>">
      <input class="px-5 py-2 bg-transparent border-2 border-zinc-700 rounded-lg outline-none" type="email" placeholder="Email" name="email" value="<%= user.email %>">
      <input class="px-5 py-2 bg-transparent border-2 border-zinc-700 rounded-lg outline-none" type="text" placeholder="Image URL" name="image" value="<%= user.image %>">
      <input class="px-5 py-2 bg-yellow-500 hover:bg-yellow-600 transition-colors rounded-lg cursor-pointer" type="submit" value="Update User">
    </form>
  </div>

</body>
</html>
